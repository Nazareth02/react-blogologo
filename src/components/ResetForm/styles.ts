import styled from "styled-components";
import { AuthSubmitButton, NameLabel, StyledHookForm, StyledInput } from "components/SignUpForm/styles";

const StyledResetForm = styled(StyledHookForm)``;

const ResetInput = styled(StyledInput)``;

const ResetEmail = styled(NameLabel)``;

const ResetSubmitButton = styled(AuthSubmitButton)``;

export { StyledResetForm, ResetInput, ResetEmail, ResetSubmitButton };
