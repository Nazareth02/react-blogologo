import styled from "styled-components";
import { Color } from "ui";

const SignInText = styled.h2`
  font-family: "Inter";
  font-style: normal;
  font-weight: 700;
  font-size: 56px;
  line-height: 80px;
  align-items: center;
  color: ${Color.FONT};
`;

export { SignInText };
